#include <stdio.h>

int main() {
    int n, i;

    printf("Enter how many natural numbers you want to print: ");
    scanf("%d", &n);

    printf("The first %d natural numbers are:\n", n);

    for(i = 1; i <= n; i++) {
        printf("%d ", i);
    }

    printf("\n");
    return 0;
}
